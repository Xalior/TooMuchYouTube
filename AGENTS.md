# AGENTS

Repository notes for automated agents:

- Build release: `npm run build`
- Refresh domains: `npm run domains:fetch`, check-only: `npm run domains:check`
- Extension source in `extension/src`, build output in `extension/dist`
- Release zip output in `releases/` using namespace from `package.json`
